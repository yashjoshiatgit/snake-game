# Build Scripts

- `build-jar.*` compiles Java sources and creates `build/libs/VegetarianSnake-1.0.0.jar`.
- `package-installer.*` creates an installer with `jpackage`.

Use `exe` type on Windows to generate a `.exe` installer.
